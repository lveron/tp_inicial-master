# Usa una imagen de Docker pre-construida con DeepFace.
FROM deepface/deepface:latest

# Establece el directorio de trabajo dentro del contenedor.
WORKDIR /app

# Copia el archivo de requisitos.
COPY requirements.txt .

# Instala las librerías de Python desde requirements.txt.
RUN pip install --no-cache-dir -r requirements.txt

# Copia el resto de tus archivos de proyecto.
COPY . .

# Define el comando que se ejecutará al iniciar el contenedor.
# Es muy probable que tu app.py sea el punto de entrada.
CMD ["python", "app.py"]
