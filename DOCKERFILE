# Usa una imagen de Docker pre-construida con DeepFace.
FROM deepface/deepface:latest

# Establece el directorio de trabajo dentro del contenedor.
WORKDIR /app

# Copia todos los archivos de tu proyecto al contenedor.
# No es necesario copiar requirements.txt si ya lo tiene la imagen.
COPY . .

# Define el comando que se ejecutará al iniciar el contenedor.
CMD ["python", "app.py"]
